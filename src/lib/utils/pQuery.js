export default obj => {

  return path => {

  };
};

// set, get(i), setAttribute, merge, remove, has, getWithDefault, length.

$("a.b[0].c").set(3);
$("a.b[0].c").get();
$("a.b[0].c").merge({ d: 4 });
$("a.b[0].c").remove();
